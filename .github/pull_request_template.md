## 변경 사항



## 체크리스트

- [ ] 기여자 목록에 추가할 내용이 있다면, 별도의 `docs: 기여자 목록에 X 추가` 커밋/PR을 만들지 않고
      `npx all-contributors-cli add <github-id> <기여유형>` 실행 후 이 PR의 커밋에 포함했다.
- [ ] 이 PR이 릴리즈라면, 버전을 수동으로 올리지 않고
      `.github/scripts/release.sh <새버전>` 으로 버전을 올렸다 (별도의 `chore: 버전 올림` 커밋을 만들지 않는다).
